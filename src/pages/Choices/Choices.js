import React from 'react';
import { Grid } from '@material-ui/core';
import { ParticipantChoice } from '../../components/ParticipantChoice';

export const Choices = () => (
  <Grid container justify="center">
    <ParticipantChoice />
  </Grid>
);
